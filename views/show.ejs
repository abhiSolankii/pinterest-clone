<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Pinterest- Your Posts</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
      crossorigin="anonymous"
    ></script>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-800 text-white">
    <%- include("./components/navbar.ejs") %>
    <div class="p-10">
      <h1 class="mb-4 text-[3rem] font-semibold">Your Posts</h1>
      <% if(user.posts.length>0){ %>
      <div class="grid grid-cols-2 gap-y-20">
        <% user.posts.forEach((post)=>{ %>
        <!-- card starts -->
        <div class="card mb-4 w-[50rem] h-[40rem] my-auto mx-auto shadow-lg">
          <img
            src="/images/uploads/<%= post.image %>"
            class="card-img-top overflow-hidden cursor-pointer"
            alt="Post Image"
          />
          <div class="card-body">
            <h5 class="card-title text-xl font-semibold"><%= post.title %></h5>
            <p class="card-text text-lg mb-1 overflow-hidden">
              <%= post.description %>
            </p>
            <a
              href="<%= post.website %>"
              class="btn btn-primary"
              target="_blank"
              >Visit Website</a
            >
          </div>
        </div>
        <!-- card ends -->
        <% }) %>
      </div>
      <% } else{ %>
      <h1 class="text-white font-bold text-[4rem]">You have not posted yet.</h1>
      <% } %>
    </div>
  </body>
</html>
